discord.py>=2.4.0
aiohttp>=3.9.0
python-dotenv>=1.0.1
openai>=1.40.0
requests>=2.31.0
# Amazon/eBay/StockX scraping (optional at runtime; enable in config)
playwright>=1.40.0
# Optional: discum (only if you want user-token forwarding)
# discum
